
sudo: required
language: java

jdk:
  - openjdk8

script:
  - mvn -DskipTests=true package

before_deploy:
    - mkdir MiningBuild
    - find . -name '*.jar' -exec cp {} ./MiningBuild \;
    - cd /home/travis/build/leosoftlima/jenkins/MiningBuild
    - tar -zcvf result.tar.gz *
deploy:
  provider: releases
  api_key: $GITHUB_TOKEN
  file: result.tar.gz
  name: fetchjar-af9ccc42bd055f1cbb9f8eb5ad156ca95dcb19da
  file_glob: true
  overwrite: false
  skip_cleanup: true
  on:
    all_branches: true 
            